# 0.1 全局 Material 3 视觉系统实施计划

## 任务 1：主题基础

- 为亮暗模式、seed color 和主要组件主题编写测试。
- 新增 `AppTheme` 与间距/尺寸常量，替换 `DndTableApp` 的裸 ThemeData。

## 任务 2：应用壳层

- 统一 NavigationBar、NavigationRail、AppBar 和页面背景。
- 验证手机、平板与桌面断点。

## 任务 3：功能区分批迁移

- 设置与服务器。
- 资料库。
- 战役与聊天。
- 角色卡与创建向导。

每批先写视觉与交互测试，再删除局部硬编码主题。

## 任务 4：全局验收

- 扫描硬编码颜色、过大圆角、嵌套卡片和不足 48px 的交互目标。
- 运行全量 Flutter 门禁和 Web release，并在 5 个目标宽度截图复核。
