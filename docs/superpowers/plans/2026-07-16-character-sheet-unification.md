# 0.1 角色卡信息架构统一实施计划

## 任务 1：锁定行为

- 在角色页面 Widget 测试中增加独立「资源」标签断言。
- 增加四个战斗状态信息板的稳定 Key 断言。
- 增加摘要与正文首段相同的法术浮层去重测试。
- 先运行目标测试并确认新断言失败。

## 任务 2：统一运行时状态

- 在 `CharacterDetailPage` 中新增本地运行时合并函数。
- HP、临时 HP、灵感、状态、死亡豁免、法术位和职业资源统一通过该函数更新当前 CharacterSheet。
- 保留现有外部持久化回调契约。

## 任务 3：重构总览与资源页

- 将 `_RuntimePanel` 中的资源定义、编辑和次数状态移入独立 `_ResourcesPanel`。
- 把总览状态 Wrap 改为响应式格式化信息板。
- 将角色详情扩展为七个标签并兼容旧默认标签设置。
- 在设置默认标签选项中加入「资源」，合并冗余的状态/详情/笔记入口。

## 任务 4：统一资料正文

- 新建共享 `ContentEntryReader` 和可单测的正文去重函数。
- 角色卡完整预览与浮动预览复用该组件。
- 资料库详情复用相同正文处理逻辑，同时保留职业等级列表、收藏和笔记。

## 任务 5：验证与预览

- 运行角色页和资料库目标测试。
- 运行 Flutter 全量测试与 `flutter analyze`。
- 构建 Web release，更新当前 5173 预览产物并检查页面加载及控制台。
- 记录本轮架构审查结论和剩余风险。
