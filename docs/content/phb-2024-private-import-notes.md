# PHB 2024 私有导入记录

## 边界

用户提供了本地 PDF：`C:\Users\26047\Downloads\龙与地下城 玩家手册 2024.pdf`。

该 PDF 只能用于用户本地和自托管服务器的私有资料库。开源仓库不得提交商业正文、完整规则描述或可再分发的内容包。

## 本次提取结果

已生成本地私有草稿：

```text
private-imports/phb-2024-index-draft.content.private.json
```

该路径已被 `.gitignore` 忽略。草稿内容为目录索引和页码，不包含正文。

统计结果：

| 类型 | 数量 | 说明 |
| --- | ---: | --- |
| `class` | 12 | 第 3 章职业目录节点 |
| `species` | 10 | 第 4 章种族目录节点 |
| `feat` | 75 | 第 5 章专长目录节点 |
| `equipment` | 13 | 第 6 章装备章节 / 表格节点 |
| `spell` | 391 | 第 7 章法术目录节点 |

## 当前限制

- 装备具体条目位于 PDF 表格中，目录只暴露章节和表格标题。本次尚未解析表格。
- 草稿中的 `description` 为空，后续需要用户在 GUI 中确认和补全。
- `sourceLabel` 为 `Private PHB 2024 PDF Index`，用于明确这是私有索引，不是开源内置内容。

## 下一步

1. 已为资料库页面增加「导入私有草稿」入口。
2. 已支持粘贴 `*.content.private.json`，展示 dry run 预览并导入到当前服务器。
3. 下一步让 DM 勾选要导入的条目，并选择保存到个人内容包或战役内容包。
4. 为装备页实现表格解析，优先解析名称、价格、重量、分类等事实字段。
5. 所有私有导入产物继续保存在 `private-imports/` 或用户服务器数据库中。
