# D&D Rules

Shared D&D rules logic will live here.

Current scope for v0.1:

- Reserve the package boundary for pure, tested rules calculations.
- Keep client-only prototypes inside the Flutter app until a rule is stable enough to share.
- Do not add broad rules abstractions before character sheets and content references need them.
